/**:
  ros__parameters:
    HALF_WHEEL_BASE: 0.7127 # HALF of the wheelbase in meters # TODO: Verify this on the final robot
    HALF_TRACK_WIDTH: 0.36585 # HALF of the trackwidth in meters # TODO: Verify this on the final robot
    STEERING_MOTOR_GEAR_RATIO: 40 # Gear ratio of the steering motor (40:1)
    FRONT_LEFT_MAGNET_OFFSET: 0 # TODO: Find this on the real robot (in encoder counts)
    FRONT_RIGHT_MAGNET_OFFSET: 0 # TODO: Find this on the real robot (in encoder counts)
    BACK_LEFT_MAGNET_OFFSET: 0 # TODO: Find this on the real robot (in encoder counts)
    BACK_RIGHT_MAGNET_OFFSET: 0 # TODO: Find this on the real robot (in encoder counts)
    GAZEBO_SIMULATION: False # Set to true if running in Gazebo
