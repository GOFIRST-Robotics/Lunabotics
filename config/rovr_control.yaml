/**:
  ros__parameters:
    autonomous_driving_power: 0.25 # Measured in Duty Cycle (0.0-1.0)
    max_drive_power: 1.0 # Measured in Duty Cycle (0.0-1.0)
    max_turn_power: 1.0 # Measured in Duty Cycle (0.0-1.0)
    digger_belt_power: -0.3 # Measured in Duty Cycle (0.0-1.0)
    digger_lift_manual_power: 0.075 # Measured in Duty Cycle (0.0-1.0)
    autonomous_field_type: "nasa" # The type of field ("top", "bottom", "nasa")
    lift_digging_start_position: -3050 # Measured in encoder counts!
    lift_digging_end_position: -100 # Measured in encoder counts!

    absolute_max_dig_cost: 200 # Measured as a Costmap grid value (0-255)
    max_dig_cost: 100 # Measured as a Costmap grid value (0-255)

    # Locations of all dig sites to check, as [x, y] pairs
    # Ros param lists must be flattened, so this is a list of all x values followed by all y values
    all_dig_locations: [1, 2, 3, 4]